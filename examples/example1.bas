5 PRINT "X VALUE", "SINE", "RESOLUTION"
10 READ D
25 LET M = -1
30 FOR X = 0 TO 3 STEP D
40 IF SIN(X) <= M THEN 80
50 LET X0 = X
60 LET M = SIN(X)
80 NEXT X
85 PRINT X0, M, D
90 GOTO 10
100 DATA 0.1, 0.01, 0.001
110 END
